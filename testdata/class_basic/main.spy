class Point:
    def __init__(self, x: int, y: int):
        self.x = x
        self.y = y

    def sum(self) -> int:
        return self.x + self.y

p: Point = Point(3, 4)
print(p.x)
print(p.y)
print(p.sum())
