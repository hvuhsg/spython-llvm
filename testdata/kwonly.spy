def announce(label: str, *items: int, mode: str) -> None:
    print(label, mode, len(items))
    for x in items:
        print(x)

announce("a", 1, 2, 3, mode="loud")
announce("b", mode="quiet")

def named(a: int, b: int) -> int:
    return a * 10 + b

print(named(b=2, a=1))
print(named(1, b=9))
