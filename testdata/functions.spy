def add(a: int, b: int) -> int:
    return a + b

def fib(n: int) -> int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

print(add(3, 4))
print(fib(10))

def greet() -> None:
    print(42)

greet()
